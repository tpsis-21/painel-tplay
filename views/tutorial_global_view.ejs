<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= tutorial.title %> - Tutorial TopPlay</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-950 text-slate-50">
    <main class="min-h-screen flex flex-col items-center justify-center px-4 py-12">
        <section class="w-full max-w-3xl bg-slate-900/80 border border-slate-700 rounded-3xl shadow-2xl p-6 md:p-8 space-y-6">
            <header class="space-y-2 text-center">
                <p class="text-xs font-semibold tracking-[0.2em] text-blue-400 uppercase">Tutorial TopPlay</p>
                <h1 class="text-2xl md:text-3xl font-black tracking-tight"><%= tutorial.title %></h1>
                <% if (tutorial.description) { %>
                    <p class="text-sm text-slate-300 max-w-2xl mx-auto"><%= tutorial.description %></p>
                <% } %>
            </header>

            <% if (tutorial.url) { %>
                <div class="rounded-2xl overflow-hidden border border-slate-700 bg-black">
                    <div class="aspect-video">
                        <video controls preload="metadata" class="w-full h-full">
                            <source src="<%= tutorial.url %>" type="video/mp4">
                            Seu navegador não suporta vídeos.
                        </video>
                    </div>
                </div>
            <% } else { %>
                <p class="text-center text-sm text-slate-300">Nenhum vídeo associado a este tutorial.</p>
            <% } %>

            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 text-xs text-slate-400">
                <p class="select-all break-all">
                    Link deste tutorial:
                    <span class="font-mono text-[11px]"><%= (typeof window !== 'undefined' ? window.location.href : '') %></span>
                </p>
            </div>
        </section>
    </main>
</body>
</html>

