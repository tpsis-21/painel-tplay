# Configuração para Node.js Selector Hostinger
# Este arquivo deve estar na raiz do domínio

RewriteEngine On

# 1. Forçar HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# 2. Ignorar arquivos físicos e diretórios
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 3. Redirecionar tudo para o Node.js (via Passenger/NodeSelector)
# O Hostinger Node Selector normalmente já cuida disso, mas se não funcionar:
# RewriteRule ^(.*)$ app.js/$1 [L]
