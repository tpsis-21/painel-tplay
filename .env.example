########################################
# Configurações básicas de servidor
########################################
# Porta onde o servidor vai rodar (em dev)
PORT=3000
# Host de bind do Node (0.0.0.0 é o padrão seguro em containers)
HOST=0.0.0.0

# URL principal do seu site (sem barra no final)
BASE_URL=https://ajuda.tplay21.in

# Ambiente (development ou production)
NODE_ENV=production

########################################
# Login do painel (NUNCA commitar real)
########################################
# Usuário e senha do painel de administração.
# Defina valores fortes diretamente no painel de variáveis
# de ambiente do servidor (EasyPanel / Nixpacks).
ADMIN_USER=admin
ADMIN_PASS=changeme

########################################
# Sessão e cookies
########################################
# Segredo usado para assinar cookies de sessão.
# Troque por uma string longa e aleatória em produção.
SESSION_SECRET=troque-esta-string-em-producao

# Se quiser forçar redirecionamento HTTP -> HTTPS atrás de proxy:
FORCE_HTTPS=true

# Se o ambiente de deploy já termina em HTTPS, mantenha true para secure cookie.
COOKIE_SECURE=true
